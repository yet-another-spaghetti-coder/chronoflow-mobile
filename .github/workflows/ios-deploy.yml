name: iOS TestFlight Deployment

on:
  push:
    branches:
      - chore/setup-ios-deploy


permissions:
  contents: read
  actions: read

jobs:
  # Job 1: Build the app (no access to signing secrets)
  build:
    runs-on: macos-latest
    environment: release
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.9'  # Pin specific version
          channel: 'stable'

      - name: Install Firebase Tools
        run: npm install -g firebase-tools

      - name: Install FlutterFire CLI
        run: flutter pub global activate flutterfire_cli

      - name: Configure Firebase
        run: |
          flutterfire configure --token=${{ secrets.FIREBASE_TOKEN }} \
            --project=${{ secrets.FIREBASE_PROJECT_ID }} \
            --platforms=android,ios \
            --yes

      - name: Get dependencies
        run: flutter pub get --enforce-lockfile
      
      
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
          working-directory: ios
      
      - name: Install Fastlane
        working-directory: ios
        run: |
          bundle install
      
      - name: Decode App Store Connect API Key
        run: |
          echo "${{ secrets.APP_STORE_CONNECT_API_KEY }}" | base64 -d > ~/${{ secrets.APP_STORE_CONNECT_KEY_PATH }}
          chmod 600 ~/${{ secrets.APP_STORE_CONNECT_KEY_PATH }}
      - name: Debug environment variables
        run: |
            echo "Checking MATCH_GIT_BASIC_AUTHORIZATION..."
            if [ -z "$MATCH_GIT_BASIC_AUTHORIZATION" ]; then
            echo "❌ MATCH_GIT_BASIC_AUTHORIZATION is EMPTY or UNSET"
            else
            echo "✅ MATCH_GIT_BASIC_AUTHORIZATION is set (length: ${#MATCH_GIT_BASIC_AUTHORIZATION})"
            # Show first 10 chars to verify it's not empty
            echo "Preview: ${MATCH_GIT_BASIC_AUTHORIZATION:0:10}..."
            fi
        env:
            MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
      
      - name: Sign and Export IPA
        working-directory: ios
        run: bundle exec fastlane beta
        env:
          APPLE_ID: ${{secrets.APPLE_ID}}
          APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_KEY_PATH: ~/${{ secrets.APP_STORE_CONNECT_KEY_PATH }}
          ARCHIVE_PATH: $HOME/build/Runner.xcarchive
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
      
      - name: Clean up secrets
        if: always()
        run: |
          rm -f ~/${{ secrets.APP_STORE_CONNECT_KEY_PATH }}
          rm -rf ~/Library/MobileDevice/Provisioning\ Profiles/*
      
      - name: Log deployment
        if: always()
        run: |
          echo "Deployment attempt at $(date)"
          echo "Commit: ${{ github.sha }}"
          echo "Actor: ${{ github.actor }}"
          echo "Status: ${{ job.status }}"